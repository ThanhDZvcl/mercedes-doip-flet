name: Build Flet APK

on:
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.38.5'

    - name: Install Flet
      run: |
        pip install --upgrade pip
        pip install flet

    - name: Build APK
      run: |
        flet pack mercedes_doip_flet.py

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: flet-apk
        path: build/**/*.apk
